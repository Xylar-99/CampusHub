

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Friend {
  id         Int      @id @default(autoincrement())
  userId     Int      @unique
  friendsId  Json?

  @@map("friends")
}
